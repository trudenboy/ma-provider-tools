name: {% if locale == 'ru' %}Отчёт об ошибке{% else %}Bug Report{% endif %}
description: {% if locale == 'ru' %}Сообщить об ошибке в провайдере {{ domain }}{% else %}Report a bug in the {{ domain }} provider{% endif %}
labels: ["incident:bug", "priority:medium"]
body:
  - type: markdown
    attributes:
      value: |
        {% if locale == 'ru' %}Спасибо, что сообщаете об ошибке! Пожалуйста, заполните как можно больше деталей.{% else %}Thanks for taking the time to report a bug! Please fill in as much detail as possible.{% endif %}

  - type: input
    id: provider_version
    attributes:
      label: {% if locale == 'ru' %}Версия провайдера{% else %}Provider version{% endif %}
      description: |
        {% if locale == 'ru' %}Найдите в `manifest.json` → поле `version`, или в теге последнего GitHub Release.{% else %}Find this in `manifest.json` → `version` field, or in the latest GitHub Release tag.{% endif %}
      placeholder: "e.g. 2.1.0"
    validations:
      required: true

  - type: input
    id: ma_version
    attributes:
      label: {% if locale == 'ru' %}Версия Music Assistant{% else %}Music Assistant version{% endif %}
      placeholder: "e.g. 2.3.1"
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: {% if locale == 'ru' %}Шаги воспроизведения{% else %}Steps to reproduce{% endif %}
      placeholder: |
        {% if locale == 'ru' %}1. Перейти в ...
        2. Нажать на ...
        3. Увидеть ошибку{% else %}1. Go to ...
        2. Click on ...
        3. See error{% endif %}
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: {% if locale == 'ru' %}Ожидаемое поведение{% else %}Expected behaviour{% endif %}
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: {% if locale == 'ru' %}Фактическое поведение{% else %}Actual behaviour{% endif %}
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: {% if locale == 'ru' %}Соответствующие логи{% else %}Relevant logs{% endif %}
      description: {% if locale == 'ru' %}Вставьте соответствующий вывод логов здесь.{% else %}Paste any relevant log output here.{% endif %}
      render: text
